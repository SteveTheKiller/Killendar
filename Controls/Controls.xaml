<!--
    Theme-INDEPENDENT control styles. All brush references use DynamicResource, so a
    live theme or accent change repaints them automatically. Merged AFTER a
    Themes/{Theme}.xaml color dictionary.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:kui="clr-namespace:Killendar.Controls">

    <!-- Shared film-grain tile. ImageSource is assigned once at startup (Chrome.ApplyGrainTexture)
         so global styles - like the context menus - can layer the same grain as the chrome/flyouts. -->
    <ImageBrush x:Key="GrainTileBrush" TileMode="Tile"
                ViewportUnits="Absolute" Viewport="0,0,256,256" Stretch="None"/>

    <!-- Floors the scrollbar thumb at a grabbable minimum. -->
    <kui:ThumbViewportFloorConverter x:Key="ThumbFloor"/>

    <!-- Typewriter wordmark font (bundled in Fonts/). The face name after '#' must match the
         font's internal family name. -->
    <FontFamily x:Key="WordmarkFont">/Killendar;component/Fonts/#Typewriter - a602 (dead postman 2004)</FontFamily>

    <!-- Global themed tooltip. Implicit (no key) so every tooltip app-wide picks it up. -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource ControlCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="{DynamicResource FlyoutShadowOpacity}"/>
                        </Border.Effect>
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataGrid Row (translucent so a pane grain shows through) -->
    <Style x:Key="DarkDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource RowAltBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource RowHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid Cell -->
    <Style x:Key="DarkDataGridCell" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0" Opacity="{DynamicResource IconShadowOpacity}"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Primary Button (filled accent) -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnPrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <!-- Hover/press are a translucent tint OVER the accent, so the feedback works
                         whatever the palette says. Killendar's 15 accent files all set
                         PrimaryHoverBrush == PrimaryBrush, so the old brush swap was a no-op and
                         this button had never shown a hover. Ported from KillerShell.
                         (2026-07-30) -->
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource ControlCornerRadius}">
                        <Grid>
                            <Border x:Name="tint" CornerRadius="{DynamicResource ControlCornerRadius}" Background="Transparent"/>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="tint" Property="Background" Value="#2EFFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="tint" Property="Background" Value="#38000000"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Outline Button (accent border + text, reactive fill on hover) -->
    <Style x:Key="OutlineButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OutlineBtnBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="14,5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OutlineRestBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource ControlCornerRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border x:Name="bevelLight" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelLightBrush}"
                                BorderThickness="{DynamicResource ButtonBevelLightThickness}"/>
                        <Border x:Name="bevelDark" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelDarkBrush}"
                                BorderThickness="{DynamicResource ButtonBevelDarkThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Order matters: IsPressed must come after IsMouseOver to win. -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource OutlineBtnBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource OnPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <!-- Outline actions stay hollow when pressed on every palette. Several
                                 shared themes define OutlinePressedBrush as translucent gray, which
                                 made the compact Install action look disabled after a theme switch. -->
                            <Setter TargetName="border" Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource OutlineBtnBrush}"/>
                            <Setter TargetName="bevelLight" Property="BorderBrush" Value="{DynamicResource BevelDarkBrush}"/>
                            <Setter TargetName="bevelDark" Property="BorderBrush" Value="{DynamicResource BevelLightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Surface Button (secondary): fill one tier lighter than what it sits on, a dim border, and a
         hover that dims rather than colors. Accent on hover belongs to OutlineButton and
         PrimaryButton; a secondary button that lights up accent reads as the primary action. -->
    <Style x:Key="SurfaceButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource InputFieldBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource ControlCornerRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border x:Name="bevelLight" IsHitTestVisible="False" BorderBrush="{DynamicResource BevelLightBrush}" BorderThickness="{DynamicResource ButtonBevelLightThickness}"/>
                        <Border x:Name="bevelDark" IsHitTestVisible="False" BorderBrush="{DynamicResource BevelDarkBrush}" BorderThickness="{DynamicResource ButtonBevelDarkThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Order matters: IsPressed after IsMouseOver so it wins. -->
                        <!-- A real fill change, not an opacity fade. Opacity 0.75 on a PaneBrush
                             button over the dark panel is a ~9-step move - invisible in practice,
                             so Cancel read as dead, with no visible hover. (2026-07-30) -->
                        <!-- Fill only, no accent border: the accent outline is the CONFIRM
                             button's identity (OutlineButton), and a neutral button borrowing it
                             on hover reads as a second confirm. (2026-07-30) -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            <Setter TargetName="bevelLight" Property="BorderBrush" Value="{DynamicResource BevelDarkBrush}"/>
                            <Setter TargetName="bevelDark" Property="BorderBrush" Value="{DynamicResource BevelLightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Chip Toggle: a small on/off pill for picking one of a short row of options (the repeat
         pattern, the weekdays, how a series ends).

         A stock CheckBox or RadioButton renders light gray on every theme, and a bare ToggleButton
         keeps WPF's DEFAULT template, which paints the system highlight blue on hover whatever the
         theme says - the same trap the all-day toggle and the sidebar close X fell into.

         CHECKED IS A FILL, not an outline. The accent OUTLINE is the confirm button's identity
         (OutlineButton); a chip wearing it would read as the primary action rather than as a
         setting that is currently on. -->
    <Style x:Key="ChipToggle" TargetType="ToggleButton">
        <Setter Property="Background" Value="{DynamicResource PaneBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Margin" Value="0,0,4,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource SmallCornerRadius}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        </Trigger>
                        <!-- After IsMouseOver so hovering a checked chip does not undo the fill. -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource OnPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <!-- Inputs take the darkest tone with a dim border, not the mid surface: the border is what
         delineates a field, not a lighter fill. -->
    <Style x:Key="DarkTextBox" TargetType="TextBox">
        <!-- SurfaceBrush (#333333 on Dark), NOT BackgroundBrush. The appointment sidebar paints
             nothing of its own and sits straight on the window's BackgroundBrush (#1c1c1c), so a
             BackgroundBrush field was the exact same color as the panel behind it - the only thing
             separating a text box from empty space was its 1px border. KillerNotes has always used
             SurfaceBrush here; this is that. (2026-07-30)
             It reads correctly on the other surfaces too: the dialog cards are PaneBrush (#3a3a3a),
             against which a SurfaceBrush field is a step darker and reads as recessed. -->
        <Setter Property="Background" Value="{DynamicResource InputFieldBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="SelectionOpacity" Value="0.3"/>
        <!-- Replaces WPF's built-in Cut/Copy/Paste menu, which is built from the framework theme
             and ignores this app's ContextMenu/MenuItem styles. DynamicResource, not Static: the
             menu is defined further down this file. -->
        <Setter Property="ContextMenu" Value="{DynamicResource TextInputContextMenu}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource ControlCornerRadius}">
                            <!-- VerticalAlignment must follow VerticalContentAlignment. Without it the
                                 content host fills the box top-down, so in a fixed-height row the text
                                 line is taller than the space left by Padding and scrolls out of sight,
                                 leaving what looks like an empty box. -->
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <Border IsHitTestVisible="False" BorderBrush="{DynamicResource BevelDarkBrush}"
                                BorderThickness="{DynamicResource CheckSunkenDarkThickness}"/>
                        <Border IsHitTestVisible="False" BorderBrush="{DynamicResource BevelLightBrush}"
                                BorderThickness="{DynamicResource CheckSunkenLightThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource InputHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Implicit TextBox style, so a box picks up the theme without per-box styling; without it
         an unstyled box falls back to WPF's white fill and blue focus border. DarkTextBox stays
         keyed as well for explicit references. -->
    <Style TargetType="TextBox" BasedOn="{StaticResource DarkTextBox}"/>

    <!-- One outline around a date and its calendar affordance. The calendar glyph is part of the
         field, not a second rounded button glued to its right edge. -->
    <Style x:Key="DateFieldShell" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource InputFieldBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource InputHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Scrollbar: thin by default, fattens via the thumb's margin on hover, no arrow buttons.
         ThumbFloor keeps the thumb grabbable on long lists. -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="12"/>
        <Setter Property="MinWidth" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track x:Name="PART_Track" IsDirectionReversed="True"
                               Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" Value="{TemplateBinding Value}">
                            <Track.ViewportSize>
                                <MultiBinding Converter="{StaticResource ThumbFloor}" ConverterParameter="64">
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ViewportSize"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Maximum"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Minimum"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualHeight"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Orientation"/>
                                </MultiBinding>
                            </Track.ViewportSize>
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PageBack" Command="ScrollBar.PageUpCommand" Focusable="False" IsTabStop="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PageForward" Command="ScrollBar.PageDownCommand" Focusable="False" IsTabStop="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Style>
                                        <Style TargetType="Thumb">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border x:Name="thumbBorder" Background="{DynamicResource ScrollThumbBrush}" CornerRadius="{DynamicResource ScrollThumbRadius}" Margin="4,0"/>
                                                        <ControlTemplate.Triggers>
                                                            <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}" Value="Horizontal">
                                                                <Setter TargetName="thumbBorder" Property="Margin" Value="0,4"/>
                                                            </DataTrigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ScrollThumbHoverBrush}"/>
                                                                <Setter TargetName="thumbBorder" Property="Margin" Value="2,2"/>
                                                                <Setter TargetName="thumbBorder" Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0" Opacity="0.3"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Thumb.Style>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- A vertical track counts downward, so it needs IsDirectionReversed;
                             a horizontal one counts rightward and must not, or dragging the thumb
                             left scrolls the content right. The paging commands flip with it. -->
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Track"   Property="IsDirectionReversed" Value="False"/>
                            <Setter TargetName="PageBack"     Property="Command" Value="ScrollBar.PageLeftCommand"/>
                            <Setter TargetName="PageForward"  Property="Command" Value="ScrollBar.PageRightCommand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="12"/>
                <Setter Property="MinHeight" Value="12"/>
                <Setter Property="Width" Value="Auto"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Context Menu (grain + rounding + shadow match the theme flyout) -->
    <!-- ═══════════ ONE definition for every flyout surface ═══════════
         Menus and flyouts used to be written twice: the ContextMenu template here, and a
         hand-rolled Border on ThemePopup in MainWindow.xaml with its own margin, its own
         background and its own grain brush. Same intent, two sets of numbers, so they drifted
         apart and only matched when someone noticed and re-tuned one by hand.
         (2026-07-30)

         Both now use these two styles, so there is nothing left to keep in sync. Change a flyout's
         look HERE and every flyout changes with it. Do not put chrome values on a flyout again. -->

    <!-- The card: fill, frame, radius, shadow-room margin and the shadow itself. -->
    <Style x:Key="FlyoutCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{DynamicResource FlyoutCornerRadius}"/>
        <!-- Margin is the room the drop shadow renders into: a Popup clips to its own bounds, so
             anything the shadow throws past it is cut off square. Sized to the effect below, not
             picked: WPF expands render bounds by the FULL BlurRadius, so a 22 blur needs 22 each
             way, and the 4px downward depth takes 4 off the top and adds 4 to the bottom. A uniform
             6 clipped every edge, and half the correct value still leaves a faint visible step.
             CONSUMERS MUST COMPENSATE: 16 wider on the left and 12 taller on the top than the old
             6, so a popup needs HorizontalOffset -16 and VerticalOffset -12 on top of whatever it
             already had, or the card drifts away from what opened it. -->
        <Setter Property="Margin" Value="22,18,22,26"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" BlurRadius="22" ShadowDepth="4" Direction="270" Opacity="{DynamicResource FlyoutShadowOpacity}"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The grain overlay. Goes LAST inside the card so it lies OVER the content, including a
         highlighted row's solid SelectionBg fill - underneath, that row is the one flat surface in
         the app. Radius 5 = the card's 6 minus its 1px border, so the texture stops exactly at the
         inside of the frame. Uses the shared GrainTileBrush; flyouts do not get private grain
         brushes wired through Chrome.cs any more. -->
    <Style x:Key="FlyoutGrain" TargetType="Border">
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="CornerRadius" Value="{DynamicResource FlyoutCornerRadius}"/>
        <Setter Property="Background" Value="{DynamicResource GrainTileBrush}"/>
        <Setter Property="Opacity" Value="{DynamicResource GrainOpacity}"/>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <!-- Compensates FlyoutCard's shadow halo so the card lands where it did when that halo was
             a uniform 6. See the note on FlyoutCard's Margin. -->
        <Setter Property="HorizontalOffset" Value="-16"/>
        <Setter Property="VerticalOffset" Value="-12"/>
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Padding" Value="0,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <!-- FADE IN. A ContextMenu has no PopupAnimation property - it is not a Popup,
                         it is hosted inside one WPF owns - so the fade is driven from the template
                         with an EventTrigger on Loaded. Submenus already faded, so a menu appeared
                         instantly while its own children faded in, which is backwards. 150ms
                         ease-out, the curve Anim.FadeIn uses. (2026-07-30) -->
                    <!-- Chrome comes from FlyoutCard - the SAME style the theme flyout uses.
                         Opacity 0 is the fade's start value, not chrome. -->
                    <Border x:Name="MenuRoot" Opacity="0" Style="{StaticResource FlyoutCard}">
                        <Grid>
                            <StackPanel IsItemsHost="True" Margin="{TemplateBinding Padding}" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            <Border Style="{StaticResource FlyoutGrain}"/>
                            <Border IsHitTestVisible="False" BorderBrush="{DynamicResource MenuBevelLightBrush}"
                                    BorderThickness="{DynamicResource MenuBevelLightThickness}"/>
                            <Border IsHitTestVisible="False" BorderBrush="{DynamicResource MenuBevelDarkBrush}"
                                    BorderThickness="{DynamicResource MenuBevelDarkThickness}"/>
                            <Border IsHitTestVisible="False" Margin="{DynamicResource MenuBevelInnerMargin}"
                                    BorderBrush="{DynamicResource MenuBevel2LightBrush}"
                                    BorderThickness="{DynamicResource MenuBevel2LightThickness}"/>
                            <Border IsHitTestVisible="False" Margin="{DynamicResource MenuBevelInnerMargin}"
                                    BorderBrush="{DynamicResource MenuBevel2DarkBrush}"
                                    BorderThickness="{DynamicResource MenuBevel2DarkThickness}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="ContextMenu.Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="MenuRoot" Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <!-- TextBrush, not MenuTextBrush: menu items are body text. See KillerUI. -->
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <!-- 8 on the left, not 24: the 16px icon gutter provides the inset now. -->
        <Setter Property="Padding" Value="8,6,10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <!-- Four columns, the family standard from KillerPDF (KillerUI/Controls):
                           0  16px gutter - MenuItem.Icon, or the check glyph when checkable
                           1  the header
                           2  InputGestureText, right-aligned
                           3  submenu chevron
                         This template used to render ONLY the header, so Icon, InputGestureText
                         and submenus were all silently dropped - which is why the themed system
                         menu came up with no icons. -->
                    <Border x:Name="bg" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}" CornerRadius="{DynamicResource SmallCornerRadius}" SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter x:Name="IconPresenter" Grid.Column="0" ContentSource="Icon"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="Check" Grid.Column="0" Text="&#xE73E;"
                                       FontFamily="Segoe MDL2 Assets" FontSize="11"
                                       Foreground="{DynamicResource PrimaryBrush}"
                                       VerticalAlignment="Center" HorizontalAlignment="Left"
                                       Visibility="Collapsed"/>

                            <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center"/>

                            <TextBlock x:Name="Gesture" Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                                       Foreground="{DynamicResource MutedTextBrush}" FontSize="11"
                                       VerticalAlignment="Center" Margin="24,0,0,0"/>

                            <TextBlock x:Name="SubArrow" Grid.Column="3" Text="&#xE76C;"
                                       FontFamily="Segoe MDL2 Assets" FontSize="8"
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       VerticalAlignment="Center" Margin="12,0,0,0"
                                       Visibility="Collapsed"/>

                            <Popup x:Name="SubPopup" AllowsTransparency="True" Placement="Right"
                                   HorizontalOffset="-20" VerticalOffset="-12" Focusable="False" PopupAnimation="Fade"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}">
                                <!-- Halo sized to the 22/4 shadow, same as FlyoutCard; the offsets
                                     above carry the -16,-12 compensation on top of the -4 gap. -->
                                <Grid Margin="22,18,22,26">
                                    <Border Background="{DynamicResource MenuBackgroundBrush}" CornerRadius="{DynamicResource FlyoutCornerRadius}"
                                            IsHitTestVisible="False">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="22" ShadowDepth="4"
                                                              Direction="270" Opacity="{DynamicResource FlyoutShadowOpacity}"/>
                                        </Border.Effect>
                                    </Border>
                                    <Border Background="{DynamicResource MenuBackgroundBrush}"
                                            BorderBrush="{DynamicResource MenuBorderBrush}"
                                            BorderThickness="1" CornerRadius="{DynamicResource FlyoutCornerRadius}" SnapsToDevicePixels="True">
                                        <Grid>
                                            <Border IsHitTestVisible="False" CornerRadius="{DynamicResource FlyoutCornerRadius}"
                                                    Background="{DynamicResource GrainTileBrush}"
                                                    Opacity="{DynamicResource GrainOpacity}"/>
                                            <StackPanel IsItemsHost="True" Margin="0,4"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="InputGestureText" Value="">
                            <Setter TargetName="Gesture" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="SubArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <!-- Background only - see KillerUI/Controls/Controls.xaml. Recoloring the
                             text to the accent on hover was the drift caught 2026-07-30. -->
                        <!-- SELECTION, not a gray wash - solid SelectionBg fill with SelectionFg
                             text, the same treatment a selected tool or tab gets. The gesture and
                             chevron are recolored too: they are MutedTextBrush at rest, which is
                             unreadable on a saturated fill. Family standard, see KillerUI.
                             (2026-07-30) -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource SelectionBg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                            <Setter TargetName="Gesture" Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                            <Setter TargetName="SubArrow" Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="Background" Value="{DynamicResource MenuBorderBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}" Margin="{TemplateBinding Margin}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Inside a menu WPF does NOT use the implicit Separator style above - it resolves
         MenuItem.SeparatorStyleKey and falls back to the stock Aero separator, which is the light
         gray line. This keyed alias is what actually applies in menus. -->
    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator"
           BasedOn="{StaticResource {x:Type Separator}}"/>

    <!-- Text-input context menu.
         The implicit ContextMenu/MenuItem styles above do NOT reach a TextBox's or PasswordBox's
         BUILT-IN Cut/Copy/Paste menu: WPF builds that one from the framework theme dictionary, not
         from application resources, so it comes up as the stock white Aero menu however well the
         rest of the app is themed. The only reliable fix is to hand the inputs
         an explicit menu, which replaces the built-in one.

         The ApplicationCommands route to whatever has focus and handle their own enable state, so
         Cut and Copy gray out with no selection and Paste grays out on an empty clipboard for free.
         x:Shared="False" gives every input its own instance rather than passing one menu around. -->
    <ContextMenu x:Key="TextInputContextMenu" x:Shared="False">
        <MenuItem Command="ApplicationCommands.Cut" Header="{DynamicResource Str_Ctx_Cut}" InputGestureText="Ctrl+X">
            <MenuItem.Icon><TextBlock Text="&#xE8C6;" FontFamily="Segoe MDL2 Assets"/></MenuItem.Icon>
        </MenuItem>
        <MenuItem Command="ApplicationCommands.Copy" Header="{DynamicResource Str_Ctx_Copy}" InputGestureText="Ctrl+C">
            <MenuItem.Icon><TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets"/></MenuItem.Icon>
        </MenuItem>
        <MenuItem Command="ApplicationCommands.Paste" Header="{DynamicResource Str_Ctx_Paste}" InputGestureText="Ctrl+V">
            <MenuItem.Icon><TextBlock Text="&#xE77F;" FontFamily="Segoe MDL2 Assets"/></MenuItem.Icon>
        </MenuItem>
        <Separator/>
        <MenuItem Command="ApplicationCommands.SelectAll" Header="{DynamicResource Str_Ctx_SelectAll}" InputGestureText="Ctrl+A">
            <MenuItem.Icon><TextBlock Text="&#xE8B3;" FontFamily="Segoe MDL2 Assets"/></MenuItem.Icon>
        </MenuItem>
    </ContextMenu>

    <!-- Card Border -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="12,8"/>
    </Style>

    <!-- Theme swatch (title-bar theme/accent picker dots). Background = the swatch color;
         BorderBrush/Thickness form the active ring (set in ThemeFlyout.cs). -->
    <Style x:Key="PanelMenuItem" TargetType="MenuItem"><Setter Property="Padding" Value="0"/><Setter Property="StaysOpenOnClick" Value="True"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="MenuItem"><ContentPresenter ContentSource="Header"/></ControlTemplate></Setter.Value></Setter></Style>
    <Style x:Key="AccentDot" TargetType="Border"><Setter Property="Width" Value="18"/><Setter Property="Height" Value="18"/><Setter Property="CornerRadius" Value="{DynamicResource AccentSwatchCornerRadius}"/><Setter Property="Margin" Value="0,0,7,0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="BorderThickness" Value="2"/><Setter Property="BorderBrush" Value="Transparent"/></Style>
    <Style x:Key="ThemeRadio" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/><Setter Property="FontSize" Value="12"/><Setter Property="Margin" Value="0,0,0,8"/><Setter Property="Cursor" Value="Hand"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RadioButton"><Border x:Name="bd" Background="Transparent" CornerRadius="{DynamicResource SmallCornerRadius}" Margin="-8,0,2,0" Padding="10,4"><DockPanel><Grid Width="16" Height="16" Margin="0,0,8,0" DockPanel.Dock="Left"><Ellipse x:Name="ring" Width="14" Height="14" Stroke="{DynamicResource DimTextBrush}" StrokeThickness="1.5"/><Ellipse x:Name="dot" Width="8" Height="8" Fill="{DynamicResource PrimaryBrush}" Visibility="Collapsed"/></Grid><ContentPresenter VerticalAlignment="Center"/></DockPanel></Border><ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True"><Setter TargetName="dot" Property="Visibility" Value="Visible"/><Setter TargetName="ring" Property="Stroke" Value="{DynamicResource PrimaryBrush}"/><Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="{DynamicResource RowHoverBrush}"/></Trigger><!-- Checked AND hovered: ring, dot and label take RadioHoverFgBrush, aliased to PrimaryBrush so twelve themes are unchanged; Sepulchre's accent is its own hover teal, so it sets white. Declared last so it wins both single triggers. --><MultiTrigger><MultiTrigger.Conditions><Condition Property="IsChecked" Value="True"/><Condition Property="IsMouseOver" Value="True"/></MultiTrigger.Conditions><Setter TargetName="ring" Property="Stroke" Value="{DynamicResource RadioHoverFgBrush}"/><Setter TargetName="dot" Property="Fill" Value="{DynamicResource RadioHoverFgBrush}"/><Setter Property="Foreground" Value="{DynamicResource RadioHoverFgBrush}"/></MultiTrigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="ThemeSwatch" TargetType="Button">
        <Setter Property="Width" Value="15"/>
        <Setter Property="Height" Value="15"/>
        <Setter Property="Margin" Value="2,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderBrush" Value="#40FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Ellipse x:Name="dot" Fill="{TemplateBinding Background}"
                             Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="dot" Property="StrokeThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="dot" Property="StrokeThickness" Value="3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Caption buttons (Segoe MDL2 glyphs, hover fill, red close). -->
    <SolidColorBrush x:Key="DangerRed" Color="#e04444"/>

    <Style x:Key="ChromeButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource CaptionButtonBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource CaptionGlyphBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="{DynamicResource CaptionButtonWidth}"/>
        <Setter Property="Height" Value="{DynamicResource CaptionButtonHeight}"/>
        <Setter Property="Margin" Value="{DynamicResource CaptionButtonMargin}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource CaptionCloseCornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border x:Name="capLight" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelLightBrush}"
                                BorderThickness="{DynamicResource ButtonBevelLightThickness}"/>
                        <Border x:Name="capDark" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelDarkBrush}"
                                BorderThickness="{DynamicResource ButtonBevelDarkThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CaptionHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="capLight" Property="BorderBrush" Value="{DynamicResource BevelDarkBrush}"/>
                            <Setter TargetName="capDark" Property="BorderBrush" Value="{DynamicResource BevelLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Close button: only the glyph reddens on hover (no red block). -->
    <!-- CAPTION close. This is NOT the same thing as DangerCloseButton and must not be made to
         match it - there are two close treatments in the family and they are deliberately
         different:

           CAPTION  (this one, title bar)  - glyph is RED AT REST, hover fills a red BLOCK with a
                                             white glyph. The Windows caption convention, and what
                                             KillerPDF has always done.
           CARD     (DangerCloseButton)    - gray glyph, only the GLYPH reddens on hover, never a
                                             block. A filled red square is wrong on a card
                                             floating over the app.

         Copied from KillerPDF 2026-07-30; Killendar had the card behavior on its caption. -->
    <Style x:Key="ChromeCloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
        <Setter Property="Foreground" Value="{DynamicResource CaptionCloseBrush}"/>
        <Setter Property="Margin" Value="{DynamicResource CaptionCloseGap}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource CaptionCloseCornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border x:Name="capLight" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelLightBrush}"
                                BorderThickness="{DynamicResource ButtonBevelLightThickness}"/>
                        <Border x:Name="capDark" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelDarkBrush}"
                                BorderThickness="{DynamicResource ButtonBevelDarkThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CaptionCloseHoverBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource CaptionCloseHoverFgBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="capLight" Property="BorderBrush" Value="{DynamicResource BevelDarkBrush}"/>
                            <Setter TargetName="capDark" Property="BorderBrush" Value="{DynamicResource BevelLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Toolbar buttons: the glyph sits DIRECTLY on the chrome, no box at rest. The outline
         appears on hover and stays on the selected view button - KillerPDF's toolbar treatment
         (2026-07-30). Unlike KillerPDF's, this sets no fixed Width/Height: the button has
         to grow for the text-beside and text-under toolbar modes, so it sizes to its content and
         only Padding changes per mode (ToolbarStyle.cs).
         BorderThickness is 1 ALWAYS, transparent at rest. If the border only existed on hover
         every button would grow 2px as the pointer crossed it and the whole bar would shuffle. -->
    <Style x:Key="ToolbarButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="9,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource ControlCornerRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="toolbarContent" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2"
                                                      Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                </ContentPresenter.Effect>
                            </ContentPresenter>
                        </Border>
                        <Border x:Name="toolLight" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelLightBrush}"
                                BorderThickness="{DynamicResource ButtonBevelLightThickness}"/>
                        <Border x:Name="toolDark" IsHitTestVisible="False"
                                BorderBrush="{DynamicResource BevelDarkBrush}"
                                BorderThickness="{DynamicResource ButtonBevelDarkThickness}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Fill only. No colored border: KillerPDF's toolbar has never had one
                             and this is the family standard (KillerUI/Controls/Controls.xaml). -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            <Setter TargetName="toolLight" Property="BorderBrush" Value="{DynamicResource BevelDarkBrush}"/>
                            <Setter TargetName="toolDark" Property="BorderBrush" Value="{DynamicResource BevelLightBrush}"/>
                        </Trigger>
                        <!-- Selected view: the glyph goes crisp; the coloured squircle becomes
                             the raised object and therefore owns the shadow. -->
                        <Trigger Property="CommandParameter" Value="selected">
                            <!-- This setter deliberately comes after IsMouseOver. A selected tab
                                 keeps its selection tile on hover; otherwise RowHoverBrush replaces
                                 the tile while SelectionFg remains active (dark-on-dark in
                                 Ectoplasm, making the glyph appear to vanish). -->
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CalendarViewSelectedBrush}"/>
                            <Setter TargetName="toolbarContent" Property="Effect" Value="{x:Null}"/>
                            <Setter TargetName="Bd" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2"
                                                      Direction="270" Opacity="{DynamicResource SelectedTileShadowOpacity}"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Card / overlay close X. Only the GLYPH reddens on hover - no filled red block, which is
         the Windows caption treatment and wrong on a card floating over the app. Same rule
         ChromeCloseButton follows for the caption X. Named to match KillerPDF, where the
         family's overlay X comes from, so the two can be compared side by side. -->
    <Style x:Key="DangerCloseButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" CornerRadius="{DynamicResource SmallCornerRadius}">
                        <ContentPresenter x:Name="Cp" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
                            <Setter TargetName="Cp" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1"
                                                       Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- About/Shortcuts close control copied from KillerScan's overlay geometry. Unlike caption
         buttons it never paints a red tile: only the X changes color on hover. -->
    <Style x:Key="OverlayCloseButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Elevation shadow: lifts panels/cards slightly off the surface. -->
    <DropShadowEffect x:Key="ShadowBar" Color="Black" BlurRadius="6" ShadowDepth="3" Direction="270" Opacity="0.38"/>

    <!-- Themed checkbox: small square, accent check, mono label. The box fill is templated
         (default SurfaceBrush) so a caller can override it where the checkbox sits on a panel of
         the same tier and would otherwise be invisible. -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="box" Width="15" Height="15" CornerRadius="{DynamicResource SmallCornerRadius}" VerticalAlignment="Center"
                                Background="{TemplateBinding Background}" BorderBrush="{DynamicResource InputBorderBrush}" BorderThickness="1">
                            <TextBlock x:Name="check" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                        Foreground="{DynamicResource CheckMarkBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="7,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource InputHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{DynamicResource CheckBoxCheckedBrush}"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar icon-rail button: bare glyph with a soft shadow, turning the accent color on
         hover. Content is the glyph; override FontFamily/FontSize for text glyphs like a "?". -->
    <Style x:Key="RailButton" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <!-- 20 logical hugs the 16px glyphs; the 24px rail column leaves only a 2px inset per
             side, so zooming scales the icons rather than blank strip. -->
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent">
                        <TextBlock Text="{TemplateBinding Content}"
                                   FontFamily="{TemplateBinding FontFamily}"
                                   FontSize="{TemplateBinding FontSize}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270"
                                                  Opacity="{DynamicResource IconShadowOpacity}"/>
                            </TextBlock.Effect>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                        </DataTrigger>
                                        <!-- Tag="on" marks a rail toggle whose panel is open. -->
                                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}" Value="on">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

<!-- ==========================================================
     FileDialog internals: row container, text tiers, view-toggle and column-header buttons,
     and the list / icons / details templates with their panels.
     ========================================================== -->

    <!-- The places / bookmarks rail. Same behavior as PickerRow but tight: PickerRow's 8,5 padding
         plus 2,1 margin made each place about 28px tall around a 16px icon, so the rail showed far
         fewer entries than it had room for. Its own style, because the file list uses PickerRow for
         rows carrying three columns of detail that need the breathing room. -->
    <Style x:Key="PickerPlaceRow" TargetType="ListBoxItem">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="bg" Background="Transparent" CornerRadius="{DynamicResource SmallCornerRadius}" Padding="6,1" Margin="2,0">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource SelectionBg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Row container: hover + accent selection. -->
    <Style x:Key="PickerRow" TargetType="ListBoxItem">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="bg" Background="Transparent" CornerRadius="{DynamicResource SmallCornerRadius}" Padding="8,5" Margin="2,1">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                        </Trigger>
                        <!-- SelectionBg/SelectionFg, the family pair - never the raw accent.
                             Same treatment as a selected menu item or tab. (2026-07-30) -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource SelectionBg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Black drop-shadow "stroke" behind white-on-accent row content. -->
    <Style x:Key="PickerRowContent" TargetType="Panel">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                <Setter Property="Effect" Value="{DynamicResource TextStroke}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Glyph: accent for folders, dim for files, white while selected. -->
    <Style x:Key="PickerGlyph" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsFolder}" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource DimTextBrush}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Entry name: normal for folders, muted for files, SelectionFg while selected. -->
    <Style x:Key="PickerName" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="ToolTip" Value="{Binding Name}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsFolder}" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Size / modified columns in details view. -->
    <Style x:Key="PickerMeta" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource DimTextBrush}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- View toggle buttons; Tag="on" marks the active mode. -->
    <Style x:Key="PickerViewBtn" TargetType="Button">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource DimTextBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="Transparent" CornerRadius="{DynamicResource SmallCornerRadius}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="on">
                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sortable column header buttons (details view). -->
    <Style x:Key="PickerColBtn" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource DimTextBrush}"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" Padding="0,3">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- LIST view row: real 16px shell icon + name, sized so columns line up when the panel
         wraps vertically. Fixed Width is what makes the columns even; without it every column
         is as wide as its longest name and the grid looks ragged. -->
    <DataTemplate x:Key="RowTemplate">
        <StackPanel Orientation="Horizontal" Style="{StaticResource PickerRowContent}" Width="210">
            <Image Source="{Binding Icon}" Width="16" Height="16" VerticalAlignment="Center"
                   Margin="0,0,8,0" SnapsToDevicePixels="True"
                   RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            <TextBlock Text="{Binding Name}" Style="{StaticResource PickerName}"/>
        </StackPanel>
    </DataTemplate>

    <!-- ICONS view: 32px icon above a centered, wrapping name. -->
    <DataTemplate x:Key="IconTemplate">
        <StackPanel Style="{StaticResource PickerRowContent}" Width="84">
            <Image Source="{Binding IconLarge}" Width="32" Height="32"
                   HorizontalAlignment="Center" Margin="0,2,0,4" SnapsToDevicePixels="True"/>
            <TextBlock Text="{Binding Name}" Style="{StaticResource PickerName}"
                       TextWrapping="Wrap" TextAlignment="Center" MaxHeight="30"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>

    <!-- Details view: name | size | modified. -->
    <DataTemplate x:Key="DetailsTemplate">
        <Grid Style="{StaticResource PickerRowContent}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Image Source="{Binding Icon}" Width="16" Height="16" VerticalAlignment="Center"
                       Margin="0,0,8,0" SnapsToDevicePixels="True"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock Text="{Binding Name}" Style="{StaticResource PickerName}"/>
            </StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding SizeLabel}" Style="{StaticResource PickerMeta}" TextAlignment="Right" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="2" Text="{Binding ModifiedLabel}" Style="{StaticResource PickerMeta}" Margin="10,0,0,0"/>
        </Grid>
    </DataTemplate>

    <!-- Details view: one row per entry, scrolls down. -->
    <ItemsPanelTemplate x:Key="PanelStack"><VirtualizingStackPanel/></ItemsPanelTemplate>

    <!-- LIST view: fills a column top to bottom, then starts the next to the right, so the
         pane scrolls horizontally. The ListBox must disable vertical scrolling or the panel
         gets infinite height and never wraps. ItemHeight keeps the columns aligned. -->
    <ItemsPanelTemplate x:Key="PanelListCols">
        <WrapPanel Orientation="Vertical" ItemHeight="26"/>
    </ItemsPanelTemplate>

    <!-- ICONS view: left-to-right, wrapping down, scrolls vertically. -->
    <ItemsPanelTemplate x:Key="PanelIconGrid">
        <WrapPanel Orientation="Horizontal" ItemWidth="96" ItemHeight="76"/>
    </ItemsPanelTemplate>



    <!-- Themed ComboBox; without one a dropdown renders as a stock white box. -->
    <Style TargetType="ComboBox">
    <!-- SurfaceBrush, matching DarkTextBox - see the note there. A combo is an input and has to
         sit on the same surface as the text boxes beside it, or a form reads as two materials. -->
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="7,3"/>
    <Setter Property="Height" Value="26"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="MaxDropDownHeight" Value="280"/>
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="ComboBox">
                <Grid>
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource ControlCornerRadius}"/>
                    <Grid Margin="{TemplateBinding Padding}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          VerticalAlignment="Center" IsHitTestVisible="False">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock"><Setter Property="TextTrimming" Value="CharacterEllipsis"/></Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <TextBlock x:Name="chev" Grid.Column="1" Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="8"
                                   Foreground="{DynamicResource DimTextBrush}" VerticalAlignment="Center" Margin="5,1,0,0"/>
                    </Grid>
                    <ToggleButton Focusable="False" ClickMode="Press"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton"><Border Background="Transparent"/></ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                    <Popup x:Name="PART_Popup" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           Placement="Bottom" AllowsTransparency="True" PopupAnimation="Fade">
                        <Border MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                Background="{DynamicResource MenuBackgroundBrush}" BorderBrush="{DynamicResource MenuBorderBrush}"
                                BorderThickness="1" CornerRadius="{DynamicResource FlyoutCornerRadius}" Margin="6">
                            <Border.Effect><DropShadowEffect Color="Black" BlurRadius="22" ShadowDepth="4" Direction="270" Opacity="{DynamicResource FlyoutShadowOpacity}"/></Border.Effect>
                            <Grid>
                                <Border IsHitTestVisible="False" CornerRadius="{DynamicResource FlyoutCornerRadius}" Background="{DynamicResource GrainTileBrush}" Opacity="{DynamicResource GrainOpacity}"/>
                                <ScrollViewer Margin="0,4" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </Popup>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource InputHoverBrush}"/></Trigger>
                    <Trigger Property="IsDropDownOpen" Value="True">
                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        <Setter TargetName="chev" Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
    </Style>
    <!-- ═══════════ Grunge ComboBox: recessed input face, grain-wrapped themed dropdown. ═══════════ -->
    <Style TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource MenuTextBrush}"/>
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Padding" Value="9,5"/>
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="ComboBoxItem">
                <Border x:Name="bg" Background="Transparent" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                    <ContentPresenter VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter TargetName="bg" Property="Background" Value="{DynamicResource MenuHoverBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
    </Style>

    <!-- ═══════════ FOLDER TREE (file dialog left pane) ═══════════ -->
    <!-- Ported from KillerShell's Controls.xaml, the family reference. The stock WPF TreeView
         brings its own chrome: a white SystemColors selection highlight, Aero triangles, and a
         ScrollViewer whose corner square between the two scrollbars is painted with the system
         Control brush. All three are replaced rather than patched. The tree sits flat on the
         dialog card (BackgroundBrush), exactly as KillerShell's sits on its chrome, so the
         expander's mask fill matches KillerShell's too. -->

    <kui:LastChildConverter x:Key="IsLastChild"/>

    <!-- Expander triangle. Two Paths rather than a glyph font so it stays crisp at any scale.
         The two states are separate geometries rather than one chevron on a RotateTransform:
         rotating about the bounding-box center swings the arms back across the connecting line,
         which draws an X over it. The FILL path paints the panel background between the arms so
         the line reads as a zigzag rather than showing through - it reaches back to x=7, past
         the line's left edge at 7.5, and carries no stroke. -->
    <Style x:Key="TreeExpander" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent" Width="16" Height="16">
                        <Canvas Width="16" Height="16">
                            <Path x:Name="arrowFill" Data="M 7,3.5 L 13,7.5 L 7,11.5"
                                  Fill="{DynamicResource BackgroundBrush}" SnapsToDevicePixels="False"/>
                            <Path x:Name="arrow" Data="M 8.5,3.5 L 13,7.5 L 8.5,11.5"
                                  StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Stroke="{DynamicResource DimTextBrush}" SnapsToDevicePixels="False"/>
                        </Canvas>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <!-- Points down, vertex landing on x=8 - the line's center. Arm length
                                 and angle match the collapsed chevron, worked out by hand: a
                                 RenderTransform cannot be reached by TargetName (MC4111). -->
                            <Setter TargetName="arrow"     Property="Data" Value="M 4,5.25 L 8,9.75 L 12,5.25"/>
                            <Setter TargetName="arrowFill" Property="Data" Value="M 4,5.25 L 8,9.75 L 12,5.25"/>
                            <Setter TargetName="arrow" Property="Stroke" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="arrow" Property="Stroke" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Node text. SelectionBg/SelectionFg pair on selection, same as the picker rows. -->
    <Style x:Key="TreeName" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="FolderTreeItem" TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="Padding" Value="2,1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <!-- 16px gutter carries the expander AND the connecting lines, so
                                 the lines land dead center on the triangle at every depth. -->
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Connecting lines. The vertical runs the full height of the node and
                             its children; the horizontal is the stub out to this node's icon.
                             For the last child the vertical is cut to an elbow (see trigger). -->
                        <Rectangle x:Name="VerLine" Grid.RowSpan="2" Width="1"
                                   HorizontalAlignment="Center" VerticalAlignment="Stretch"
                                   Fill="{DynamicResource TreeLineBrush}" SnapsToDevicePixels="True"/>
                        <!-- Only drawn for a node with no expander - where there IS one, the
                             chevron is the connector. -->
                        <Rectangle x:Name="HorLine" Height="1" Margin="8,0,0,0"
                                   VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                   Visibility="Collapsed"
                                   Fill="{DynamicResource TreeLineBrush}" SnapsToDevicePixels="True"/>

                        <ToggleButton x:Name="Expander" Style="{StaticResource TreeExpander}"
                                      ClickMode="Press" VerticalAlignment="Center"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"/>

                        <Border x:Name="Bd" Grid.Column="1" CornerRadius="{DynamicResource SmallCornerRadius}" Margin="2,0,0,0"
                                Background="Transparent" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="PART_Header" ContentSource="Header"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Grid.Column="1"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <!-- No children means no chevron, so the elbow stub takes over as the
                             connector. The vertical stays either way: a leaf still belongs to
                             the run of siblings drawn down the gutter. -->
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="HorLine" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" SourceName="Bd" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                        </Trigger>
                        <!-- SelectionBg, the family pair - not KillerShell's RowSelectedBrush,
                             because in THIS dialog the tree node and the places row mean the same
                             thing and must light up the same way. -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectionBg}"/>
                        </Trigger>
                        <!-- Last sibling: cut the vertical to an elbow. Bound to the container
                             itself so a recycled container re-evaluates (FolderTree.cs). -->
                        <DataTrigger Value="True"
                                     Binding="{Binding RelativeSource={RelativeSource Self}, Converter={StaticResource IsLastChild}}">
                            <Setter TargetName="VerLine" Property="VerticalAlignment" Value="Top"/>
                            <Setter TargetName="VerLine" Property="Height" Value="11"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollViewer with NO corner element. The stock template puts a Rectangle in the gap
         where the two scrollbars meet, filled with SystemColors.ControlBrushKey - white in
         every theme this app ships. The cell is simply left empty here. -->
    <Style x:Key="BareScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollContentPresenter Grid.Row="0" Grid.Column="0"
                                                Margin="{TemplateBinding Padding}"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                CanHorizontallyScroll="False"
                                                CanVerticallyScroll="False"/>

                        <!-- The PART_ names are what ScrollViewer wires its own scrolling to, so
                             Value binds one-way and the control drives it from there. -->
                        <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Row="0" Grid.Column="1"
                                   Orientation="Vertical" Cursor="Arrow"
                                   Minimum="0" Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                        <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Row="1" Grid.Column="0"
                                   Orientation="Horizontal" Cursor="Arrow"
                                   Minimum="0" Maximum="{TemplateBinding ScrollableWidth}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>

                        <!-- Row 1 / Column 1 deliberately left empty: that is the corner. -->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FolderTreeView" TargetType="TreeView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource FolderTreeItem}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                        <ScrollViewer Style="{StaticResource BareScrollViewer}"
                                      Padding="{TemplateBinding Padding}"
                                      CanContentScroll="False"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
